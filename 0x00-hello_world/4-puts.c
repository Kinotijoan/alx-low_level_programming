#include <stdio.h>

/**
 * main - Entry point
 * Description:prints a certain string
 * puts(write a line or string to the output)
 * Return: Always 0 (success)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);

}
